<UserControl x:Class="SwiftPanel.Controls.ToolbarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Button Command="{Binding Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Background="Transparent" BorderThickness="1" BorderBrush="Transparent"
            Padding="4,2" Margin="1,0" Cursor="Hand"
            ToolTip="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#D0E4F8"/>
                        <Setter Property="BorderBrush" Value="#7090B8"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#A8C8E8"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="16" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="9" HorizontalAlignment="Center"/>
        </StackPanel>
    </Button>
</UserControl>
